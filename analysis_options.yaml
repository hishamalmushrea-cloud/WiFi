include: package:flutter_lints/flutter.yaml

analyzer:
  plugins:
    - custom_lint
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/generated/**"
  errors:
    invalid_annotation_target: ignore

linter:
  rules:
    # ── قواعد الجودة الإلزامية حسب مواصفات المشروع ──
    # prefer_const_constructors: true   # معطّلة مؤقتاً لتصغير سجل analyze — تُستعاد فور كشف التحذيرات المخفية
    # prefer_const_declarations: true   # معطّلة مؤقتاً لتصغير سجل analyze — تُستعاد فور كشف التحذيرات المخفية
    prefer_final_locals: true
    avoid_print: true
    # require_trailing_commas: true   # معطّلة مؤقتاً لتصغير سجل analyze — تُستعاد فور كشف التحذيرات المخفية
    always_declare_return_types: true
    annotate_overrides: true
    avoid_empty_else: true
    avoid_relative_lib_imports: true
    avoid_returning_null_for_void: true
    avoid_slow_async_io: true
    avoid_types_as_parameter_names: true
    cancel_subscriptions: true
    close_sinks: true
    discarded_futures: false
    hash_and_equals: true
    no_adjacent_strings_in_list: true
    no_duplicate_case_values: true
    no_logic_in_create_state: true
    prefer_final_in_for_each: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_single_quotes: true
    sort_child_properties_last: false
    unawaited_futures: true
    unnecessary_await_in_return: true
    # unnecessary_const: true   # معطّلة مؤقتاً لتصغير سجل analyze — تُستعاد فور كشف التحذيرات المخفية
    unnecessary_new: true
    unnecessary_null_aware_assignments: true
    use_build_context_synchronously: true
    use_decorated_box: true
    use_full_hex_values_for_flutter_colors: true
